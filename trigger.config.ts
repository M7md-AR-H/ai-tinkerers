import { defineConfig } from "@trigger.dev/sdk";

export default defineConfig({
  // Replace with your project ref: Trigger.dev dashboard -> your project -> Project settings.
  project: "proj_REPLACE_ME",
  dirs: ["./trigger"],
  maxDuration: 300,
});
